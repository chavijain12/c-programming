#include <stdio.h>

int main() {
    int num, temp, sum = 0;
    scanf("%d",&num);
    temp = num;
    while(temp>0) {
        int d = temp % 10;
        sum += d * d *d;
        temp /= 10;
        
    }
    printf(sum == num ?"armstrong":"not armstrong");
    return 0;
}
