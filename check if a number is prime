#include <stdio.h>

int main() {
    int n, i = 2, isprime = 1;
    scanf("%d",&n);
    while(i <= n/2) {
        if(n % i == 0) {
            isprime = 0;
            break;
        }
        i++;
        
    }
    printf(isprime && n > 1? "prime":"not prime");


    return 0;
}
