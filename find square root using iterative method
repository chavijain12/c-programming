#include <stdio.h>

int main() {
    float n, guess;
    scanf("%f",&n);
    guess = n/2;
    int i = 0;
    while(i < 10) {
        guess = (guess + n/guess)/2;
        i++;
    }
    printf("%f",guess);

    return 0;
}
